import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // Адрес сервера
    urlServer: 'http://localhost:3000'
  },
  mutations: {},
  actions: {},
  modules: {}
});
